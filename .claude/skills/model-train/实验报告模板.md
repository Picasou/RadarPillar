# <模型名> (<数据集>) 训练报告

> **文档定位**：<一句话定位，含 cfg / 数据集 / best ckpt 评估口径>
> **数据来源**：`output/train_log/<数据集>/<时间戳>_<模型>_<tag>/`
> **评估口径**：<R11 / R40 之类，写明 IoU 与类别阈值>

---

## 一句话总结

> **Best ckpt `<best.pth>` 当前 ep<N> 复测 <mAP=数值>，与文件名 / 论文 / 参考 存在 <±X.XX> 点 gap；类别选择性 <一句话定性>；<稳定性 / 物理直觉 的一句话定性>。**

- 关键 metric：…
- 与对照的差距：…
- 类别排序：…
- 稳定性证据：…

---

## 一、实验设置

| 项 | 内容 |
|---|---|
| CFG | `<cfg 路径>` |
| 训练脚本 | `<脚本>`（bs=N, ep=M, fromzero / resumed） |
| 数据 | `<数据集> (<split 描述>)`，<FOV / gt_sampling / placeholder 等关键开关> |
| 训练目录 | `<OUTPUT_ROOT>` |
| eval | <单点 ep / 末 N epoch 全表> |
| 评估指标 | <R11 + R40 双口径，若单口径写明> |
| seed | `<FIX_RANDOM_SEED 值>`（False=非确定性；True 且有 seed 需注明） |
| optimizer | `<adamw / adam_onecycle 等> | LR=<x> | WD=<x> | decay=[..]` |
| 参数量 | `<X.XXXX> M (可训练 <X.XXXX> M)`（来源：`record` 子命令实测 / `count_params`） |
| 计算量 | `<X.XXX> GMACs ≈ <X.XXX> GFLOPs (bs=N)`（来源：`record` 子命令 thop 实测） |

**结构对齐结论**（与参考实现 / 论文的偏差）：

- 整体 backbone（VFE / BEV / Head）层数与论文一致 / 偏差…
- 参数量差异主要来自 `<段>`，占比 …%

---

## 二、核心结果

### 2.1 best-epoch 主表（ep<N>）

| 类别 | 论文/参考 | 复现 (ep<N>) | Δ |
|---|---|---|---|
| Car @<IoU> | <数值> | <数值> | ±X.XX |
| Pedestrian @<IoU> | <数值> | <数值> | ±X.XX |
| Cyclist @<IoU> | <数值> | <数值> | ±X.XX |
| **mAP** | **<数值>** | **<数值>** | ±X.XX <绿/红区> |

> mAP 计算口径核对：mAP = (Car + Ped + Cyc) / 3 = <验算> ✅
> **R40 口径（参考）**：Car=<...>, Ped=<...>, Cyc=<...>, mAP_R40=<...>

> 容差规则（spec）：≤2 点绿区；>2 点红区必 debug。

### 2.2 ep N–M 全表（稳定性 / 证据）

| 来源 | ep | Car / Ped / Cyc / mAP | 备注 |
|---|---|---|---|
| **<本 run 标识>** | **<N>** | **<...>** | **本报告 best ckpt** |
| <对照 run 1> | <...> | <...> | <cfg / NMS / seed 差异> |
| <对照 run 2> | <...> | <...> | <...> |

**结论**：<跨 run 稳定性证据 / 反证>

---

## 三、关键排查结论

### 判据 1：类别选择性（物理可解释性）
- <Car / Ped / Cyc 排序>
- ✅ / ⚠️ 物理直觉方向 <定性>
- 无明显类间错位或塌缩

### 判据 2：跨 epoch 稳定性
- ⚠️ / ✅ <是否覆盖稳定性证据>
- 间接旁证：<...>

### 判据 3：checkpoint 文件名 vs 当前 eval gap
- 文件名 `<best_xxx.pth>` 暗示历史 <数值>
- 当前 ep<N> 重新 eval 得 <数值>，gap <±X.XX>
- 可能原因（**未验证**）：
  1. 历史 eval 用了不同的 val split / 不同的 NMS / 不同的 cfg
  2. 文件名是早期一个偶然 best 时的记录，后来未刷新
  3. 不可完全排除：<...>
- **不可完全排除**：<最可能的原因>

**结论**：<是否立即重训 / 下一步 debug 方向>

---

## 四、评价

### 达成的
- ✅ <实现 / cfg / 评估 / 类别排序 / 参数量统计…>

### 未达预期的
- 🔴 <gap / 类别退化 / 参数量偏差…>

### gap 归因（工程因素，非结构错误）
- **评估实现差异**：<...>
- **cfg 实现差异**：<...>
- **数据 split / 增强口径**：<...>
- **不可完全排除**：<...>

---

## 五、不符预期的后续操作（debug 路径，优先级从高到低）

1. **<第一步，最高优先级>**
   - <具体动作>
2. **<第二步>**
   - <...>
3. **<第三步>**
   - <...>
4. ...
5. ...

**当前停止点**：停在第 <N> 步之前 — <已完成动作 / 待用户授权的下一步>。

---

## 六、产物清单

### Checkpoint / Eval 结果
- **best ckpt**：`<OUTPUT_ROOT>/best.pth`（<MB>）
- **eval 结果**：`<OUTPUT_ROOT>/eval/<...>/`
  - `results.json`（<R40 指标>）
  - `result.pkl`（<N> 帧原始预测）
  - `log_eval_<时间戳>.txt`（…）

### 可视化

> 一列一张图，点图片可在新标签打开原图。所有图存于 **`<OUTPUT_ROOT>/asset/`**（与报告同目录），报告内用相对路径 `asset/xxx.png` 引用。
>
> **Loss 曲线来源说明**：<取自哪次 run / 是否同 best ckpt 一致>。

#### Loss 曲线

![<模型> loss curve](asset/loss_curve.png)

![<模型> tb loss curves](asset/tb_loss_curves.png)

#### BEV 帧（GT vs Pred，采样）
> 每帧包含：点云（BEV 投影）+ GT 框 + Pred 框（score ≥ <阈值>）。<彩色框说明>。N 帧均匀采样自 val 集。

![<模型> frame 00000](asset/<模型>_frames/frame_00000.png)

![<模型> frame 00185](asset/<模型>_frames/frame_00185.png)

![<模型> frame 00370](asset/<模型>_frames/frame_00370.png)

![<模型> frame 03586](asset/<模型>_frames/frame_03586.png)

![<模型> frame 04813](asset/<模型>_frames/frame_04813.png)

![<模型> frame 04998](asset/<模型>_frames/frame_04998.png)

![<模型> frame 08295](asset/<模型>_frames/frame_08295.png)

![<模型> frame 08480](asset/<模型>_frames/frame_08480.png)

### 配置 / 脚本
- 训练脚本：`<路径>`
- 模型 CFG：`<路径>`
- 数据 CFG：`<路径>`
- 参数量自检：`<脚本>`

---

## 附：mAP 口径说明

本文统一采用 **<R11 / R40>（<描述>）**，与 <论文 / 内部对照> 口径一致。

- 复现 <模型> ep<N> mAP = (Car@<...> + Ped@<...> + Cyc@<...>) / 3
                       = <...> ≈ <...>

- **同 ckpt <另一口径>（参考）**：mAP_<...> = <...>

- **checkpoint 文件名口径**：`<best_xxx.pth>` 与上述两口径的差异，来源未本地留存，**待 §五 步骤 1 验证**。

> ⚠️ 勿与 <其它口径> 混淆。
