# RadarPillar 训练任务 Lessons Learned

> 持续累积的训练 / 评测 / 排查经验。每章独立成文，可在后续工作中追加新章节。

## 目录

- **Chapter 1**：[radarpilar 复现问题排查与 Cyclist AP 暴跌定位](#chapter-1)
- **Chapter 2**：[center 头 BEV 反超 2d 头的根因：z+h 联合监督重塑共享特征](#chapter-2)
- *Chapter 3：（占位）— 待补充*

---

# Chapter 1：radarpilar 复现问题排查与 Cyclist AP 暴跌定位

> 时间：2026-07-17 ~ 2026-07-19 · 状态：已完成

## 1.1 摘要

**问题**：v1 Cyclist 3D AP 17.50，远低于论文 66.4。
**根因**：5-class 聚合把 bicycle、rider、moped_scooter、motor 合并进 Cyclist，验证集 GT 从 1434 膨胀到 6886，0.18M 模型扛不住任务膨胀——本质是评测口径不一致，不是模型问题。
**修复**：CLASS_MAPPINGS 改回 3 类 `[Car, Pedestrian, Cyclist]` + 复现 0709 训练参数 → v3 = 64.56，复现度 97.2%。

| 项 | plan 默认 | v3 | 理由 |
|---|---|---|---|
| CLASS_MAPPINGS | 5 类 | **3 类** | **关键修复** |
| BATCH_SIZE | 8 | 16 | 与 0709 一致 |
| LR_WARMUP | True | False | 与 0709 一致 |
| NMS_THRESH | 0.15 | 0.1 | 与 0709 一致 |
| EPOCHS | 80 | 80 | 用户指定 |

## 1.2 排查：4 步排除法

| 步骤 | 验证 | 结果 | 结论 |
|---|---|---|---|
| ① 数据 | raw → 聚合 infos | pre 6685 / post 32338 | 聚合生效但**不是 bug** |
| ② 评测 | cv2 vs shapely，10,650 对 | diff 3.5e-6，NMS 残留 0/47,843 | CPU eval 不是 bug |
| ③ 预测分布 | GT vs PRED 统计 | z 中心 -0.005 vs -0.686（偏低 0.7m，所有类一致）| 数据/评测不能解释 |
| ④ **复现实验（关键）** | 同一份 0709 ckpt predictions 用 3 类 / 5 类 GT 重测 | **3 类=66.95 ✅ vs 5 类=15.30** | **5-class 聚合是 100% 根因** |

## 1.3 实验记录

| 模型 | 参数量 | 训练/推理 GT | Car | Ped | Cyclist | mAP |
|---|---:|---|---:|---:|---:|---:|
| 0709（历史参考）| 0.18M | 3 类 | 37.92 | 35.60 | 66.95 | 46.82 |
| base（历史）| 0.18M | 3 类 | — | — | 69.22 | 48.72 |
| v1 (plan 基线)| 0.18M | 5 类 | 36.37 | 35.19 | 17.50 | 29.69 |
| v2 (修复尝试)| 0.18M | 5 类 | — | 34.14 | 15.89 | 28.01 |
| v3 (复现 0709)| 0.18M | 3 类 | 35.95 | 33.70 | 64.56 | 44.74 |

## 1.4 论文参考数字

来源：[RadarPillars arXiv:2408.05020](https://arxiv.org/html/2408.05020v1) Table I/IV/V。

**Table I**（frame 设定，v1 的对比目标）

| 设定 | Car AP50 | Ped AP25 | Cyclist AP25 | mAP |
|---|---:|---:|---:|---:|
| 1-frame baseline | 36.0 | 35.5 | 66.4 | 46.0 |
| 3-frame | 40.2 | 39.2 | 71.8 | 50.4 |
| 5-frame | 41.1 | 38.6 | 72.6 | 50.7 |
| 1-frame corridor | 69.4 | 47.1 | 85.4 | 67.3 |
| 5-frame corridor | 70.5 | 52.3 | 87.9 | 70.5 |

Table IV/V 要点：E=128 最优（mAP 42.9 vs E=32 base 39.6）；backbone (64,64,64) 42.6，PointPillars 4.84M 仅 39.5。完整数字见论文链接。

## 1.5 关键经验

1. **排除法 + 反事实实验**：数据 → 评测 → 预测分布 → 模型逐层排除；多假设并存时，同一 ckpt 换 GT/换配置重测才能锁定根因——AP 数字本身无法定位问题
2. **看分布不看数字**：GT vs PRED 统计（z、置信度、IoU）能定位异常
3. **5-class aggregation 不是无害的**：任务膨胀，0.18M 模型扛不住；论文靠 0.27M+ backbone + 5-frame
4. **plan 与参考实现必须对齐**：v1 错引 1-frame 66.4 当 5-frame 72.6
5. **评分阈值暴露问题**：Cyclist 0.25 最宽松 + 物体最矮 + 模型最小 → 三重不利叠加
6. **训练参数小差异大影响**：bs 8→16、warmup、NMS 0.15→0.1，单项 ~9% AP

> 产出物：v1–v3 train_log 与 infos 备份已清理，不复存在；`tools/test_cpu.py`（CPU eval 交叉验证工具）仍在。

---

# Chapter 2：center 头 BEV 反超 2d 头的根因：z+h 联合监督重塑共享特征

> 时间：2026-08-14 ~ 2026-08-15 · 状态：已完成（seed=42 重跑验证 + 3-agent fan-out 分析）

## 2.1 摘要

**问题**：center 头（8-cat，监督 xy+z+dim3+rot）BEV AP 反超 2d 头（6-cat，无 z/h），旧表 Pedestrian +7.16，而 BEV IoU 只用 xy/长宽/朝向——z/h 不进评估，何来增益？
**根因**：z+h **联合**监督的梯度回流共享 trunk，迫使特征编码"相对地面的垂直剖面"——这是 BEV 投影上不可见、但区分行人（立柱）与地杂波（贴地薄片）的关键判别维。增益体现在**检出（召回）**而非框精度。
**真实幅度**：+0.9~2.5 Ped（非 +7.16；旧数含阈值交互 ~1.3 + seed 高位抽样）。

## 2.2 实验设计（5 臂受控，seed=42，80ep，末 10 ckpt eval）

| 配置 | 监督 | loc 标度 | Ped best | Ped 末10均值 | BEV mean |
|---|---|---|---:|---:|---:|
| 2d | 6-cat | 6 | 40.84 | 40.75±0.14 | 51.87 |
| abl_z (+z) | 7 | 7 | 40.50 | 40.47±0.15 | 49.72 |
| abl_h (+h) | 7 | 7 | 40.89 | 40.72±0.19 | 50.52 |
| **center (+z+h)** | 8 | 8 | **43.06** | **42.74±0.63** | **52.59** |
| lossup (2d×1.333) | 6 | 8 | 42.27 | 40.92±0.50 | 51.84 |

lossup = 纯 2d 监督、CODE_WEIGHTS 放大 8/6 倍，loc_loss 量级精确对齐 center（实测 0.727 vs 0.714）——隔离"量级"变量。

## 2.3 根因机制链（每环有实测）

1. **不在解码层**：center 框精度持平或略差（matchIoU 0.523 vs 0.531、dimErr 0.095 vs 0.094、rec@0.5 持平）。"2d 专项更贴合考卷"只在输出层成立，而分差不在此层。
2. **在检出层**：增益全部来自召回——Ped rec@0.25 +1.5pt、检出数 +13%；PR 分解 = recall 天花板多撑 2 格（+2.5 AP）− 高分端 precision 损失（−0.7）。headline +2.22 中 ~1.3 是 SCORE_THRESHOLD=0.2 与分数分布的交互，~0.9 是真检出增益。
3. **通路是共享特征**：各 head 并联在同一 trunk 上，z/h 的 L1 梯度回流重塑 trunk 特征（监督是"特征的老师"，非推理时的注意力提示）。独立旁证：Car rotErr 0.253→0.136（减半）——rot 监督各配置完全相同，仅 z+h 联合出现，说明特征变好让其他 head 受益。
4. **为什么必须联合（捷径分析）**：
   - 单 z：绝对高度的方差被场景共模主导（Ped z 帧间 std 0.689 vs 帧内 0.257），便宜解 = 记住"这帧地面在哪" → 特征学到噪声 → **有害**（Cyc rec −4.6pt）
   - 单 h：与类别强耦合（行人 1.57/车 1.75，类内 std 0.12-0.19），便宜解 = 私有 dim 分支拿现成类别特征查表 → 不逼特征学新东西 → **no-op**
   - z+h 联合：可解码每目标 (bottom, top)=(z∓h/2)，两条捷径同时失效，特征必须真实表达垂直剖面
5. **类别选择性 = 余量**：Ped 是检出最弱类（rec 0.50、雷达 4.6 点/gt），特征改善直接转召回；Car 已饱和走质量通道（rot 减半）；Cyc −2.2 系高分端 precision 拖累（val GT 仅 1434）。

**排除项**：loss 权重放大被双杀——数学上 Adam+解耦 wd 逐位吸收均匀缩放（梯度探针 cos=1.000000），实验上 lossup 复刻量级与 hm:loc 配比而无增益（best 42.27 系 max-of-noisy-tail 伪影，+1.35 vs 期望 +0.73）。

## 2.4 名词速查（本轮讨论涉及）

| 术语 | 含义 |
|---|---|
| trunk | 共享主干（PillarVFE→…→shared_conv），所有 head 并联其上、梯度汇入其权重 |
| matchIoU / dimErr | 配对框的平均 IoU / 尺寸误差——量"框得准不准"，不量"找到多少" |
| recall / precision | 真目标中找到的比例 / 报出目标中为真的比例 |
| rec@X | 以 IoU≥X 判定"找到"的召回率；0.25 宽松、0.5 严格。增益只在 rec@0.25 → 多检出粗略目标 |
| PR 曲线 / R40 / AP | 阈值从高到低的 precision-recall 权衡轨迹；R40 = recall 轴 40 采样格；每多撑 1 格 ≈ +2.5 AP |
| 共模 | 帧内所有目标一起漂的分量（地面/安装角），对目标判别无贡献 |
| run 内 / run 级 | 同次训练相邻 ckpt 间波动（σ 0.14-0.63） / 不同训练（换 seed）间波动（4.6-9.4），差一个量级 |

## 2.5 统计教训

1. **seed 是抽样器不是放大器**：方差是任务属性（center 的增益=边缘检出，R40 台阶式 ±2.5 跳变），seed 只决定抽到分布哪个位置。旧 47.90 = 宽分布（均值 ~43）的高位抽样。
2. **单 seed 单 ckpt 不可作点估计**：末 10 ckpt 均值能压 run 内噪声，压不住 run 级噪声；跨 run 报数需 ≥3 seed 配对。
3. **BEV mean 口径脆弱**：1/3 权重押最噪的 Cyclist（跨 run 漂 9.4），跨配置比较以 per-class 为准。
4. **评估阈值是隐变量**：SCORE_THRESHOLD 0.2→0.1 使 gap +2.22→+0.90——分数分布与阈值的交互可制造/抹掉 1 点级"效应"。

## 2.6 置信度与判决实验

| 结论 | 置信度 |
|---|---|
| 排除 loss 放大 | ~95% |
| 存在真实正效应 +0.9~2.5 | ~75%（n=1 run） |
| 机制 = 共享特征→召回 | ~90% |
| "垂直剖面/捷径"具体载荷 | ~40%（假设） |

判决实验：① stop-grad 隔离 z/h 分支（增益应消失→一锤定音）；② 场景归一化 z 单独监督（应不再伤害 Cyc）；③ ≥3 seed 配对（gap 定数）。

## 2.7 关键经验

1. **共享 backbone 下，监督信号是特征的老师**：不考的科目（z/h）能提升考的科目（BEV 检出），前提是该科目逼出考纲需要的能力（垂直剖面判别）
2. **单任务监督常存在捷径解**：z 可走场景共模、h 可走类别查表；联合 (bottom, top) 才无可绕路——设计辅助监督时先问"有没有便宜解法"
3. **增益归因先分离"检出"与"框精度"**：rec@宽/严双阈值 + matchIoU/dimErr 三件套，一步定位效应在哪个能力上
4. **对照实验要隔离量级与内容**：lossup 复刻 loss 量级而不改内容，才能证明"有效成分是监督内容不是梯度大小"
5. **跨 run 方差必须实测**：run 内 σ 不是误差棒；报告单 seed 结论一律加"方向性"保留

> 产出物：5 run（output/train_log/vod/*head_b1_*/，含 best/eval×10/resbag）；完整报告 output/train_log/vod/_analysis_head_bev_rootcause.md；本轮 seed=42（tools/train.py:87，历史默认 666）。

---

# Chapter 3：（占位）

> 状态：待补充
